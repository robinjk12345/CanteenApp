<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.canteen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Controller"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Fcrit Store"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Options"
            android:label="options" />
        <activity
            android:name=".Food"
            android:label="food" />
        <activity android:name=".FirstScreen" />
        <activity android:name=".SecondScreen" />
        <activity android:name=".ThirdScreen" />
        <activity android:name=".XeroxMain" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".UserAreaActivity" />
        <activity android:name=".Stationary1"></activity>
    </application>

</manifest>